import styles from "./CartSectiom.module.scss";

const CartSection = () => {
  return (
    <section className="container">
      <h2>Product 5</h2>
    </section>
  );
};

export default CartSection;
